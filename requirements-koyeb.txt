# ============================================================
# Koyeb (Micro Instance 512 MB) — requirements runtime.
#
# Menginstal PyTorch versi CPU-only (--extra-index-url pytorch/whl/cpu)
# untuk mengoptimalkan waktu build dan memori RAM (menghindari wheel
# CUDA yang membengkakkan image hingga beberapa GB).
#
# File ini MIRROR dari requirements.txt root, dengan perbedaan hanya
# pada sumber wheel torch (CPU-only). Setelah torch ter-pin dari index
# CPU, sisa paket lain tetap diambil dari PyPI.
# ============================================================
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.111.0
uvicorn>=0.30.1
pydantic>=2.7.4
pydantic-settings>=2.3.4
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
supabase>=2.0.0
rdkit>=2023.9.5
torch>=2.3.1
torch-geometric>=2.5.3
scipy>=1.13.1
numpy>=2.0
scikit-learn>=1.7.2
cachetools>=5.3.3

# Paket ml (hepatwin-ml) — featurization/model/explain runtime
# (lihat requirements.txt root untuk penjelasan; Dockerfile menyalin ml/).
-e ./ml
