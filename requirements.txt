# INFERENCE-ONLY (masuk Docker image). AGENTS.md §3.9: jangan tambah pustaka
# training/plotting/notebook ke sini — ukuran image berdampak ke NFR PRD §6.
# Catatan: torch + torch-geometric adalah dependensi inference HANYA bila
# ML_BACKEND=gnn. Bila gerbang T1.11 memilih 'tabular', keduanya dipindah ke
# requirements-dev.txt dan diganti lightgbm di sini (lihat Arsitektur §D.5, §F.1).
fastapi>=0.111.0
uvicorn>=0.30.1
pydantic>=2.7.4
pydantic-settings>=2.3.4
rdkit>=2023.9.5
torch>=2.3.1
torch-geometric>=2.5.3
scipy>=1.13.1
numpy>=1.26.4
shap>=0.45.1
