BACKEND_CORS_ORIGINS=["*"]
AI_MODEL_PATH="models/model.pt"
CACHE_DB_PATH="cache.db"
DEBUG=True

# PERINGATAN: JANGAN aktifkan MOCK_MODE=True di deployment produksi.
# Hanya untuk pengembangan frontend selama konstanta PD belum tervalidasi
# Farmasi (PRD §13 item #1) — /simulate akan mengembalikan response dummy
# (model_status="mock", DILI_score=0.5) alih-alih hasil nyata.
MOCK_MODE=False